openapi: 3.0.3
info:
  title: Scenario API
  version: "1.0"
paths:
  /users:
    post:
      requestBody:
        required: false
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                type: string
