# scenario example

Run all checks:

```bash
xdiff run examples/scenario/xdiff.yaml
```

List checks:

```bash
xdiff run --list examples/scenario/xdiff.yaml
```

Run only the JSON check:

```bash
xdiff run --only local-user-json examples/scenario/xdiff.yaml
```
