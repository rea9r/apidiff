# spec example

Compare two OpenAPI specs:

```bash
xdiff spec examples/spec/old-openapi.yaml examples/spec/new-openapi.yaml
```

Show canonical diff paths:

```bash
xdiff spec --show-paths examples/spec/old-openapi.yaml examples/spec/new-openapi.yaml
```
