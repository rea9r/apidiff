openapi: 3.0.3
info:
  title: Example API
  version: "1.0"
paths:
  /users:
    post:
      requestBody:
        required: true
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                type: integer
    get:
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                type: string
